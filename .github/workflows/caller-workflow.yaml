name: Caller Workflow

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize]

jobs:
  call_reusable_workflow:
    uses: ./.github/workflows/reusable-workflow.yaml
    with:
      some_input: "Hello from caller workflow"
